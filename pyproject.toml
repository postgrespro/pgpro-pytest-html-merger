[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pgpro-pytest-html-merger"
version = "0.2.0"
description = "A tool to merge multiple pytest-html reports into a single one"
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"

authors = [
    {name = "Postgres Professional", email = "info@postgrespro.ru"}
]

dependencies = [
    "beautifulsoup4>=4.11.1",
    "packaging>=21.0",
]

[project.scripts]
# Теперь точка входа выглядит короче и логичнее
pgpro-pytest-html-merger = "pgpro_pytest_html_merger.__main__:cli"

[tool.setuptools.packages.find]
where = ["src"]
# Указываем новое имя папки
include = ["pgpro_pytest_html_merger*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
log_file_level = "NOTSET"
log_file_format = "%(levelname)8s [%(asctime)s] %(message)s"
log_file_date_format = "%Y-%m-%d %H:%M:%S"

[tool.flake8]
extend-ignore = ["E501"]
exclude = [".git", "__pycache__", "env", "venv"]
